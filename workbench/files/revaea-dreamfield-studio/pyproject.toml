[project]
name = "revaea-dreamfield-studio"
version = "0.1.0"
description = "Interactive studio for recoloring and exporting Revaea Dream Field icons."
requires-python = ">=3.11"
dependencies = [
    "pillow",
    "pyside6",
]
